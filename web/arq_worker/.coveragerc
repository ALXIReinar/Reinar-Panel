[run]
# Измеряем покрытие ТОЛЬКО для web/arq_worker (subscription сервис)
source = web/arq_worker

# Исключить из измерения покрытия
omit =
    # Тесты
    web/arq_worker/tests/*
    */tests/*
    */test_*.py
    
    # Конфигурационные файлы
    web/arq_worker/config.py
    web/arq_worker/arq_config.py
    web/arq_worker/utils/arq_logger_config.py

    # Entry points
    web/arq_worker/main.py
    
    # Python служебные файлы
    */__init__.py
    */__pycache__/*
    web/arq_worker/__pycache__/*
    web/arq_worker/*/__pycache__/*
    web/arq_worker/*/*/__pycache__/*
    
    # Virtual environments
    */.venv/*
    */venv/*
    
    # Pytest cache
    web/arq_worker/.pytest_cache/*

# Использовать относительные пути
relative_files = True

[report]
# Показывать строки которые не покрыты тестами
show_missing = True

# Точность процентов (2 знака после запятой)
precision = 2

# Сортировка по проценту покрытия
sort = Cover

# Пропустить пустые файлы
skip_empty = True

# Исключить из отчёта (игнорируемые строки)
exclude_lines =
    # Стандартные исключения
    pragma: no cover
    def __repr__
    def __str__
    raise AssertionError
    raise NotImplementedError
    
    # Entry points
    if __name__ == .__main__.:
    
    # Type checking
    if TYPE_CHECKING:
    from typing import TYPE_CHECKING
    
    # Abstract methods
    @abstractmethod
    @abc.abstractmethod
    
    # Логирование (не тестируем логи)
    log_event
    logger\.
    print\(
    
    # Defensive programming
    pass

[html]
# Директория для HTML отчёта
directory = web/arq_worker/htmlcov

[xml]
# Файл для XML отчёта (для CI/CD интеграций)
output = web/arq_worker/coverage.xml
