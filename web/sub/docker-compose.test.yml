name: test_reinar_panel_sub

# Используется в CI для тестирования
# IMAGE_TAG передается из GitHub Actions с SHA коммита (например: abc1234)
services:
  pg_db:
    image: postgres:16
    container_name: pg_db
    environment:
      - POSTGRES_USER=${PG_ADMIN}
      - POSTGRES_PASSWORD=${PG_ADMIN_PASSWORD}
      - POSTGRES_DB=test_${PG_DB}
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${PG_ADMIN}" ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s
    restart: on-failure
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ../secrets/test_dumps:/docker-entrypoint-initdb.d:z

  redis:
    image: redis:7.2-alpine
    container_name: redis
    command:
      - redis-server
      - --requirepass
      - ${REDIS_PASSWORD}
      - --appendonly
      - "yes"
    volumes:
      - redis_data:/data
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}

  sub-service:
    image: reinar_panel/sub_service:${IMAGE_TAG}
    command: python -m pytest web/sub/tests -v
    container_name: test_sub-service
    depends_on:
      pg_db:
        condition: service_healthy
    environment:
      - ENV_FILE=/run/secrets/env.sub.test
    secrets:
      - env.sub.test


secrets:
  env.sub.test:
    file: .env.sub.test

volumes:
  pgdata:
    driver: local
  redis_data:
    driver: local